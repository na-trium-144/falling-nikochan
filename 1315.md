現状では、POST /api/result と GET /og/result のパラメータを誰でも偽造可能である。しかし、認証を実装して鍵をフロントエンド内に埋め込んだとしても理論上解析可能だし開発者ツールから見える。そこで開発者ツールを使っても鍵がバレにくい認証方法として以下を考える

1. <del>秘密鍵Aをサーバー側の環境変数にセットし、公開鍵Aをpublic/に置く。</del> 秘密鍵と公開鍵をサーバー側の環境変数にセットし、APIで公開鍵を公開する。 (形式はJWKsとか?)
    - 何らかの理由で鍵をローテーションした場合に過去の公開鍵も公開できる形式が良い (日付付き?)
2. frontendのビルドのたびに秘密鍵Bと公開鍵Bを生成し、秘密鍵Bをフロントエンドのenvにセットし、公開鍵Bをpublic/以下に生成する。(jwk? spki?)
    - 秘密鍵は難読化してDefinePluginで埋め込む、crypto.subtle.signを使わずにpure js実装のライブラリを使う
4. frontendで /[locale]/play にアクセスした際、秘密鍵Cと公開鍵Cを生成し、公開鍵CをBで署名して POST /api/playSession に送る。(JWT形式でできる)
5. サーバーは/api/playSessionにおいてそれをBで検証し、公開鍵CをAで署名したトークンTを返す。(JWT形式でできる)
6. frontendは POST /api/record する際に、ResultParamのデータと、ResultParamのCによる署名と、Tを送る。(Tは`Authorization: Bearer JWT`で送り、残りをjson?)
7. サーバーは/api/recordにおいてTをAで検証し、ResultParamをCで検証し、ResultParamに含まれる時刻を検証する。そして、レスポンスでResultParamのAによる署名を返す。
    - /api/recordがDB保存と署名の2個の役割を持つことになる... 分けるか?
    - ただしautoの場合は保存をして署名をせず、playbackRate!=1の場合は保存をせず署名はする
    - ResultParam単体を使い回すのでこれはJWTじゃないほうがわかりやすいかも
    - msgpackエンコードって一意じゃない可能性があるな? msgpackでリクエスト送らないとダメか
8. ResultParamといっしょにそのAによる署名を保存・共有する。
9. サーバーの /og/result, /share, frontendの /[locale]/share/placeholder は、ResultParam + Aによる署名 を受け取ってAで検証する。

- 秘密鍵Bは難読化されたfrontendのjsコードのどこかに含まれるが、解析されたとしても次のビルドで無効
- 5以降Bは使用しないので新しいバージョンがビルドされておりBが変わっていたとしても問題ない
- 3のリクエストまたはレスポンスが開発者ツールで覗かれても、秘密鍵Cはわからないので使えない
- 5のリクエストまたはレスポンスが開発者ツールで覗かれても、ResultParamに時刻が含まれるのでその瞬間以外無効
- POST /api/recordのリクエストスキーマを変更しResultParamの全情報を含める必要がある。後方互換性は不要、完全にスキーマを書き換えてしまって良い。ResultParamの形式はResultSerializedSchemaをJSONにして送り、署名時にmsgpackシリアライズする
- ResultParamのスキーマを変更し時刻(分単位とかでよさそう)+署名を含める必要がある。こっちは後方互換性が必要で、現在ver3まであるスキーマにver4を追加する。署名はresultとは別のsignクエリパラメータで渡す。ver3以下は署名なしでok、ver4以降は署名がないとエラーとする


以上の仕様をプロンプトに与えてgeminiに書かせてみたが、微妙

honoのjwtミドルウェア(のutilの関数)を使ったり、鍵の形式をjwkにしたりできるはず

TODO: Discordでは画像だけコピーして共有される場合もあるが、その場合に検証できる方法はなにかあるか?